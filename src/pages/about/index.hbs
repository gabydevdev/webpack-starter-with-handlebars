{{!-- {{assign title='about us'}} --}}

{{! define the block content: include the `about/content.hbs` partial }}
{{#partial 'content'}}
{{> about/content}}
{{/partial}}

{{! extends the layout template with blocks defined above }}
{{> layout}}
